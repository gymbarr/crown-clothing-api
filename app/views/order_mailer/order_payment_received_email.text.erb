<% user_orders_url = "#{FRONTEND_HOST}:#{FRONTEND_PORT}/orders" %>
<%= "Hello #{@user.username}" %>
===============================================
<%= "Dear #{@user.username}, we have received the payment for your order №#{@order.id}" %>
You can monitor its status in your profile:
<%= user_orders_url %>
Best regards, the administration of Crown Clothing shop